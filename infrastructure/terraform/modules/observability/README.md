# Observability

Owns the workflow dashboard, operational alarm topic and policy, native AWS metric alarms,
structured-log metric filters, and Outbox EMF alarms. Inputs are names and identifiers rather than
whole service resources, keeping the module independent from the application topology.
